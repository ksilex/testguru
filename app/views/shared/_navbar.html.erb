<nav class="navbar navbar-expand-lg py-2 navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= root_path %>">TestGuru</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if current_user&.admin? %>
          <li class="nav-item">
            <%= link_to t("layouts.admin.head"), admin_tests_path, class: "nav-link" %>
          </li>
        <% end %>
        <li class="nav-item">
            <%= link_to t("all_tests"), tests_path, class: "nav-link" %>
        </li>
      </ul>
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <% if current_user %>
          <span class="navbar-text text-info">
            <%=t("entered_as")%> <%=current_user.username%>
          </span>
          <li class="nav-item">
            <%= link_to t("sign_out"), destroy_user_session_path, method: :delete, class: "nav-link" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to t("devise.sessions.new.head"), new_user_session_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to t("devise.registrations.new.head"), new_user_registration_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
